# codex-switch

`codex-switch` is a local wrapper around the Codex CLI. It selects the best
logged-in Codex account instance before forwarding each `codex ...` command.

Status: design phase. The initial architecture is documented in
`docs/superpowers/specs/2026-04-06-codex-switch-design.md`.
